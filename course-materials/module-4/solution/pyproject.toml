[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "inventory"
version = "0.1.0"
description = "Idempotent inventory adjustment processor"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/inventory"]

[tool.mypy]
strict = true
