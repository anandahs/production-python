[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "signupflow"
version = "0.1.0"
description = "Signup record validation and reporting - Module 2 lab solution"
requires-python = ">=3.11"
dependencies = [
    "pydantic==2.13.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/signupflow"]

[tool.mypy]
strict = true
