[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pipeline"
version = "0.1.0"
description = "Production-grade ingestion and transformation service"
requires-python = ">=3.11"
dependencies = [
    "click==8.1.7",
    "pydantic==2.13.4"
]

[project.scripts]
pipeline = "pipeline.cli:cli"

[tool.hatch.build.targets.wheel]
packages = ["src/pipeline"]

[tool.mypy]
strict = true